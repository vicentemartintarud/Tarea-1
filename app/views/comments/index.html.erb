<p id="notice"><%= notice %></p>

<h1><%= @story.title %></h1>
<h3><%= @story.header %></h3>
<p><%= @story.body %></p>

<table>
  <thead>
    <tr>
      <th>Author</th>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @comments.each do |comment| %>
      <tr>
        <td><%= comment.author %></td>
        <td><%= comment.content %></td>
        <td><%= link_to 'Editar Comentario', edit_story_comment_path(@story, comment) %></td>
        <td><%= link_to 'Eliminar Comentario', story_comment_path(@story, comment), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<br>

<%= link_to 'Nuevo Comentario', new_story_comment_path %>
<%= link_to 'Noticias', stories_path %>
